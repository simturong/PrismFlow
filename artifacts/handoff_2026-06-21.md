# PrismFlow Handoff — 2026-06-21 00:45

## 진행 상황 (Progress)

### 이번 세션 완료 항목
- **Phase 7-8: 창 제어 버튼 시인성 개선** (`overlay.py`) — 커밋 `a978cc6`
  - `Segoe MDL2 Assets` 폰트를 1순위로 적용하여 Windows 표준 벡터 아이콘으로 렌더링
  - 최소화(`\uE921`), 최대화(`\uE922`), 복원(`\uE923`), 닫기(`\uE8BB`) MDL2 코드포인트 매핑
  - 평상시 글자색 `#cbd5e1` → `#e2e8f0`으로 밝기 강화
  - 닫기 버튼 평상시 `#ff6b6b` (붉은 톤) 차별화, hover 시 `#e81123` 빨간 배경
  - hover 배경 `0.08` → `0.12`로 피드백 강화
  - 전체 회귀 테스트 **51 passed, 3 skipped** ✅

### 누적 완료 현황
- Phase 1~7: **전체 완료** (7-1 ~ 7-8 모두 `[x]`)
- Phase 8: 8-1(pyannote 오프라인), 8-2(build_release.py) 완료. **8-3(Inno Setup) 미착수.**
- Phase 6-3-5(이중 검증·다인 실회의): `[/]` 진행 중 상태 유지

## 실패한 접근 방식 (Trial & Error)
- 이전 세션에서 유니코드 기호(`—`, `□`, `❐`, `✕`)를 `Segoe UI` 폰트로 렌더링 시도 → 10px 사이즈에서 기호가 투명하게 표시되어 실질적으로 보이지 않음
- QSS `text` 속성으로 기호 제어 시도 → QSS에서 `text` 속성 미지원, `setText()` 직접 호출로 우회
- QFrame 컨테이너 레이아웃으로 버튼 영역 가림 현상 → QFrame 제거하고 다이렉트 레이아웃으로 해결

## 현재 직면한 문제 (Blockers)
- **Claude CLI Code 1 에러**: 세션 시작 직후 `cli_controller` 실행 실패(Code 1, stderr 비어있음) 발생. 세션 리밋일 수도 있고 다른 원인일 수도 있음. Phase 7-2/7-3 Fallback 모드가 탑재되어 있으므로 크래시는 방지되나, 실제 원인 규명 필요.
- **Phase 6-3-5 다인 실회의 검증**: 아직 미완. 복수 화자 환경에서의 화자분리 정확도 실측 필요.

## 다음 세션을 위한 구체적인 목표 (Next Goals)
1. **Phase 8-3**: Inno Setup (`setup.iss`) 스크립트 작성 및 단일 설치파일(`PrismFlow_Setup_v1.0.exe`) 빌드 검증
2. **Claude CLI 에러 원인 조사**: stderr가 비어 있는 Code 1 실패의 근본 원인 파악 (세션 리밋 vs 다른 에러)
3. **(선택) Phase 6-3-5**: 다인 실회의 환경에서 STT 화자분리 정확도 실측 및 vad/모델 튜닝
